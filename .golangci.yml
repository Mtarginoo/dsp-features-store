version: "2"

linters:
  default: standard
  settings:
    errcheck:
      # Writing to an http.ResponseWriter has no recovery path: by the time it
      # can fail, the status line and headers are already on the wire.
      exclude-functions:
        - fmt.Fprintf
        - fmt.Fprintln
